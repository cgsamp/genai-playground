// frontend/app/(dashboard)/summaries/layout.tsx
export default function SummariesGroupedLayout({
                                                   children,
                                               }: {
    children: React.ReactNode
}) {
    return children;
}
